<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-12">
      <% if flash[:success] %>
        <div class="alert alert-success alert-dismissible fade show text-center" role="alert">
          <%= flash[:success] %>
        </div>
      <% end %>  
      <div class="card">
        <div class="card-body">
          <h1 class="card-title text-center">Listado de Documentales</h1>
          <table class="table table-striped">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Sinopsis</th>
                <th>Director</th>
              </tr>
            </thead>
            <tbody>
              <% @documentary_films.each do |documentary_film| %>
                <tr>
                  <td><%= documentary_film.name %></td>
                  <td><%= documentary_film.synopsis %></td>
                  <td><%= documentary_film.director %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%= link_to "Agregar nuevo Documental", new_documentary_film_path, class: "btn btn-primary mt-3" %>
        </div>
      </div>
    </div>
  </div>
</div
